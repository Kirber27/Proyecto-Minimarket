# Copia este archivo a .env y completa con las credenciales del proyecto Supabase.
# La clave anon es publica por diseno: la RLS protege los datos, no el secreto.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
